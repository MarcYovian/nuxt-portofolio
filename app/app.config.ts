export default defineAppConfig({
  ui: {
    colors: {
      primary: "amber",
      neutral: "zinc",
    },
    button: {
      defaultVariants: {
        size: "md",
        color: "primary",
        variant: "solid",
      },
    },
  },
});
